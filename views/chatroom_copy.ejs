
<!doctype html>
<html lang="ko">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-fQybjgWLrvvRgtW6bFlB7jaZrFsaBXjsOMm/tB9LTS58ONXgqbR9W8oWht/amnpF" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js" ></script>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" integrity="sha384-zCbKRCUGaJDkqS1kPbPd7TveP5iyJE0EjAuZQTgFLD2ylzuqKfdKlfG/eSrtxUkn" crossorigin="anonymous">

    <title>chatting(<%= chat.title %>)</title>
    <style>
        .chat-content {
            height: 450px;
            overflow-y: scroll;
            padding: 10px;
        }
        .chat-content li {
            margin-top: 10px;
            list-style: none;
        }
        .text-small {
            font-size: 12px;
            color: gray;
            margin-bottom: 0;
        }
        .chat-box {
            background: #eee;
            padding: 5px;
            border-radius: 5px;
            float: left;
        }
        .mine {
           float: right;
        } 

    </style>

    <script>
      $(document).ready(function(){
        var targetId = $("#targetId").val();
        var eventSource;
        eventSource = new EventSource('/chatConnection'+targetId);
        eventSource.addEventListener('test',function(e){
          console.log(JSON.parse(e.data));
        });
      });
        
      
      // 채팅 보내기
      function chatSubmit(){
        var chat = $("#chat-input").val(); // 채팅 내용
        var targetId = $("#targetId").val(); // 대화 상대

       

        $.ajax({
            method : 'post'
            , url : '/chat/memoSubmit'
            , data : {chat:chat
                      , targetId:targetId
                      }
          }).done(function(result){
            if(result.message=='success'){
              alert('메시지 보내기 성공!');
              var html ='';
              html = '<li><span class="chat-box mine">'+chat+'</span></li>';
              $(".chat-content").append(html);
              $("#chat-input").val('');
            }else{
              alert('메시지 전송에 실패하였습니다!');
            }
            
          }).fail(function(xhr, textStatus, errorThrown){
            
            console.log(xhr, textStatus, errorThrown);
   
          });
      }
    </script>
  </head>
  <%- include('nav.html')%>

  <body>
    
  
<input type="hidden" name="targetId" id="targetId" value="<%= targetId %>">
<div class="container p-4 detail">

    <div class="row">
       <div class="col-9 p-0">
         <div class="chat-room">
            <ul class="list-group chat-content">
              <%  
                if(chat!=null){
                 for(var i=0;i < chat.length;i++){ 
                  if(chat[i].userId==userId){%>
                    <li><span class="chat-box mine"><%= chat[i].chat %></span></li> 
                <%}else{%>
                    <li><span class="chat-box"><%= chat[i].chat %></span></li>
                <%}
                }  
              }%>
             
            </ul>
          <div class="input-group">
            <input class="form-control" id="chat-input" name ="chat">
            <button class="btn btn-secondary" id="send" onclick="javascript:chatSubmit();">전송</button>
          </div>
        </div>
      </div>
    </div>
  
  </div> 
  </body>
</html>
